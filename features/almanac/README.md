# Almanac

This feature owns almanac and daily calendar logic. The active engine implementation lives under `engine/`.

The active public API is `/api/v1/almanac/*`, and admin usage records are under `/api/v1/internal/almanac/*`.
