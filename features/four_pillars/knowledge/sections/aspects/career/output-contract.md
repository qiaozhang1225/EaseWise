# Career Output Contract

必须输出 JSON object：
- `aspect_key`: 必须为 `career`。
- `title`: 一句话标题。
- `score`: 必须等于 locked facts 中本专项分数。
- `content`: 事业适配、成长路径和可用资源。
- `risk`: 职场压力、规则冲突或合作消耗提醒。
- `elements_check`: 必须包含 `日主`、`五行`、`十神`、`合冲刑害`、`喜忌` 五个键。
