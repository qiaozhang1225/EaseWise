# Family Environment Output Contract

必须输出 JSON object：
- `aspect_key`: 必须为 `family_environment`。
- `title`: 一句话标题。
- `score`: 必须等于 locked facts 中本专项分数。
- `content`: 家庭环境倾向、居住感受和调整建议。
- `risk`: 环境压力、家庭互动或居住细节提醒。
- `elements_check`: 必须包含 `日主`、`五行`、`十神`、`合冲刑害`、`喜忌` 五个键。
